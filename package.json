{
  "name": "focus-kusa-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "homepage": "https://focuskusa.org",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "optimize": "node scripts/optimize-images.mjs",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sharp": "^0.34.5",
    "vite": "^6.2.0"
  }
}
